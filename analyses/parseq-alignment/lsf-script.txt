#BSUB -P project
#BSUB -J parseq-alignment
#BSUB -oo job.out -eo job.err
#BSUB -n 1
#BSUB -R "rusage[mem=2GB] span[hosts=1]"
#BSUB -cwd "."
# Optional: email when this launcher job completes (child jobs use CONTACT_EMAIL from project_parameters.Config.yaml)
#BSUB -N
#BSUB -u user.name@stjude.org

########################################
# Run 
bash run-parseq-alignment.sh