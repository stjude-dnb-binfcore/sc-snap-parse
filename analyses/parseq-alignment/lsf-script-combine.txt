#BSUB -P project
#BSUB -J parseq-combine
#BSUB -oo job-combine.out -eo job-combine.err
#BSUB -n 1
#BSUB -R "rusage[mem=2GB] span[hosts=1]"
#BSUB -cwd "."
# Optional: email when this launcher job completes (combine child job uses CONTACT_EMAIL from project_parameters.Config.yaml)
#BSUB -N
#BSUB -u user.name@stjude.org

########################################
# Combine only (no alignment step)
bash run-splitpipe-combine.sh
